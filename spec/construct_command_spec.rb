require "_helpers"

describe "Wraith config to CLI argument mapping" do
  describe "construct_command" do
    it "passing variables to construct_command outputs correct command" do

    end
  end
end
